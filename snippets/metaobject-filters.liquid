{% comment %}
  Renders metaobject-based filters

  Usage:
  {% render 'metaobject-filters' %}
{% endcomment %}

{%- liquid
  # Simulate metaobject filters since they are created in admin
  assign size_filter = 'Size'
  assign size_values = shop.metaobjects['shopify--size'].values

  assign color_filter = 'Color'
  assign color_values = shop.metaobjects['shopify--color-pattern'].values

  assign activities_filter = 'Activities'
  assign activities_values = shop.metaobjects.product_feature.activities.values

  assign material_filter = 'Material'
  assign material_values = 'Cotton,Polyester,Nylon,Wool,Down' | split: ','

  assign features_filter = 'Features'
  assign features_values = 'Waterproof,Breathable,Windproof,Insulated,Lightweight' | split: ','

  assign activity_filter = 'Activity'
  assign activity_values = 'Hiking,Skiing,Running,Climbing,Casual' | split: ','
-%}

{% comment %} Size Filter {% endcomment %}
<details
  id="Details-metaobject-size-{{ section.id }}"
  class="{% if filter_type == 'horizontal' %}disclosure-has-popup facets__disclosure{% else %} facets__disclosure-vertical{% endif %} js-filter accordion"
  data-index="size"
  {% if filter_type == 'vertical' %}
    open
  {% endif %}
>
  <summary class="py-4">
    <div class="summary__title">
      <h2 class="h4 accordion__title inline-richtext">{{ size_filter }}</h2>
    </div>
    {{- 'icon-caret.svg' | inline_asset_content -}}
  </summary>
  <div class="accordion__content parent-display {% if filter_type == 'horizontal' %}facets__display{% else %}facets__display-vertical{% endif %}">
    <fieldset class="facets-wrap parent-wrap">
      <ul class="facets-layout facets-layout-list facets__list list-unstyled" role="list">
        {%- for size in size_values -%}
          {%- assign product_count = size.referenced_by | size -%}
          <li class="list-menu__item facets__item">
            <label class="facets__label">
              <input
                type="checkbox"
                name="filter.v.meta.size"
                value="{{ size.label | handleize }}"
                id="Filter-size-{{ forloop.index }}"
              >
              <span class="facet-checkbox__text">{{ size.label }} ({{ product_count }})</span>
            </label>
          </li>
        {%- endfor -%}
      </ul>
    </fieldset>
  </div>
</details>

{% comment %} Color Filter {% endcomment %}
<details
  id="Details-metaobject-color-{{ section.id }}"
  class="{% if filter_type == 'horizontal' %}disclosure-has-popup facets__disclosure{% else %} facets__disclosure-vertical{% endif %} js-filter accordion"
  data-index="color"
>
  <summary class="py-4">
    <div class="summary__title">
      <h2 class="h4 accordion__title inline-richtext">{{ color_filter }}</h2>
    </div>
    {{- 'icon-caret.svg' | inline_asset_content -}}
  </summary>
  <div class="accordion__content parent-display {% if filter_type == 'horizontal' %}facets__display{% else %}facets__display-vertical{% endif %}">
    <fieldset class="facets-wrap parent-wrap">
      <ul class="facets-layout facets-layout-list facets__list list-unstyled" role="list">
        {%- for color in color_values -%}
          <li class="list-menu__item facets__item">
            <label class="facets__label">
              <input
                type="checkbox"
                name="filter.v.meta.shopify--color-pattern"
                value="{{ color.label | handleize }}"
                id="Filter-color-{{ forloop.index }}"
              >
              <span class="facet-checkbox__text">{{ color.label }}</span>
            </label>
          </li>
        {%- endfor -%}
      </ul>
    </fieldset>
  </div>
</details>

{% comment %} Color Filter {% endcomment %}
<details
  id="Details-metaobject-activities-{{ section.id }}"
  class="{% if filter_type == 'horizontal' %}disclosure-has-popup facets__disclosure{% else %} facets__disclosure-vertical{% endif %} js-filter accordion"
  data-index="activities"
>
  <summary class="py-4">
    <div class="summary__title">
      <h2 class="h4 accordion__title inline-richtext">{{ activities_filter }}</h2>
    </div>
    {{- 'icon-caret.svg' | inline_asset_content -}}
  </summary>
  <div class="accordion__content parent-display {% if filter_type == 'horizontal' %}facets__display{% else %}facets__display-vertical{% endif %}">
    <fieldset class="facets-wrap parent-wrap">
      <ul class="facets-layout facets-layout-list facets__list list-unstyled" role="list">
        {%- for activity in activities_values -%}
          <li class="list-menu__item facets__item">
            <label class="facets__label">
              <input
                type="checkbox"
                name="filter.v.meta.features.activities"
                value="{{ activity | handleize }}"
                id="Filter-activities-{{ forloop.index }}"
              >
              <span class="facet-checkbox__text">{{ activity }}</span>
            </label>
          </li>
        {%- endfor -%}
      </ul>
    </fieldset>
  </div>
</details>

{% comment %} Material Filter {% endcomment %}
<details
  id="Details-metaobject-material-{{ section.id }}"
  class="{% if filter_type == 'horizontal' %}disclosure-has-popup facets__disclosure{% else %} facets__disclosure-vertical{% endif %} js-filter accordion"
  data-index="material"
>
  <summary class="py-4">
    <div class="summary__title">
      <h2 class="h4 accordion__title inline-richtext">{{ material_filter }}</h2>
    </div>
    {{- 'icon-caret.svg' | inline_asset_content -}}
  </summary>
  <div class="accordion__content parent-display {% if filter_type == 'horizontal' %}facets__display{% else %}facets__display-vertical{% endif %}">
    <fieldset class="facets-wrap parent-wrap">
      <ul class="facets-layout facets-layout-list facets__list list-unstyled" role="list">
        {%- for value in material_values -%}
          <li class="list-menu__item facets__item">
            <label class="facets__label">
              <input
                type="checkbox"
                name="filter.v.meta.material"
                value="{{ value | handleize }}"
                id="Filter-material-{{ forloop.index }}"
              >
              <span class="facet-checkbox__text">{{ value }}</span>
            </label>
          </li>
        {%- endfor -%}
      </ul>
    </fieldset>
  </div>
</details>

{% comment %} Features Filter {% endcomment %}
<details
  id="Details-metaobject-features-{{ section.id }}"
  class="{% if filter_type == 'horizontal' %}disclosure-has-popup facets__disclosure{% else %} facets__disclosure-vertical{% endif %} js-filter accordion"
  data-index="features"
>
  <summary class="py-4">
    <div class="summary__title">
      <h2 class="h4 accordion__title inline-richtext">{{ features_filter }}</h2>
    </div>
    {{- 'icon-caret.svg' | inline_asset_content -}}
  </summary>
  <div class="accordion__content parent-display {% if filter_type == 'horizontal' %}facets__display{% else %}facets__display-vertical{% endif %}">
    <fieldset class="facets-wrap parent-wrap">
      <ul class="facets-layout facets-layout-list facets__list list-unstyled" role="list">
        {%- for value in features_values -%}
          <li class="list-menu__item facets__item">
            <label class="facets__label">
              <input
                type="checkbox"
                name="filter.v.meta.features"
                value="{{ value | handleize }}"
                id="Filter-features-{{ forloop.index }}"
              >
              <span class="facet-checkbox__text">{{ value }}</span>
            </label>
          </li>
        {%- endfor -%}
      </ul>
    </fieldset>
  </div>
</details>

{% comment %} Activity Filter {% endcomment %}
<details
  id="Details-metaobject-activity-{{ section.id }}"
  class="{% if filter_type == 'horizontal' %}disclosure-has-popup facets__disclosure{% else %} facets__disclosure-vertical{% endif %} js-filter accordion"
  data-index="activity"
>
  <summary class="py-4">
    <div class="summary__title">
      <h2 class="h4 accordion__title inline-richtext">{{ activity_filter }}</h2>
    </div>
    {{- 'icon-caret.svg' | inline_asset_content -}}
  </summary>
  <div class="accordion__content parent-display {% if filter_type == 'horizontal' %}facets__display{% else %}facets__display-vertical{% endif %}">
    <fieldset class="facets-wrap parent-wrap">
      <ul class="facets-layout facets-layout-list facets__list list-unstyled" role="list">
        {%- for value in activity_values -%}
          <li class="list-menu__item facets__item">
            <label class="facets__label">
              <input
                type="checkbox"
                name="filter.v.meta.activity"
                value="{{ value | handleize }}"
                id="Filter-activity-{{ forloop.index }}"
              >
              <span class="facet-checkbox__text">{{ value }}</span>
            </label>
          </li>
        {%- endfor -%}
      </ul>
    </fieldset>
  </div>
</details>
