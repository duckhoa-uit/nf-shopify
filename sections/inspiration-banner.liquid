{%- liquid
  if section.settings.title != blank
    assign title = section.settings.title
  else
    assign title = 'sections.main_product.inspiration' | t
  endif
  assign image = section.settings.image
  assign image_alt = section.settings.image_alt | default: title
-%}

<!-- Inspiration Banner Section -->
<div class="inspiration-banner-section w-full page-width">
  <h2 class="text-base lg:text-2xl font-semibold text-[#0F0F0F] mb-6">
    {{ title }}
  </h2>
  <div class="inspiration-banner-container">
    <div
      class="inspiration-banner-media w-full relative overflow-hidden"
      style="padding-bottom: 56.25%;"
    >
      {% if image %}
        <img
          src="{{ image | image_url: width: 1500 }}"
          alt="{{ image_alt }}"
          loading="lazy"
          width="1500"
          height="844"
          class="absolute inset-0 w-full h-full object-cover"
        >
      {% else %}
        <img
          src="{{ 'product-banner.webp' | asset_url }}"
          alt="{{ image_alt }}"
          loading="lazy"
          width="1500"
          height="844"
          class="absolute inset-0 w-full h-full object-cover"
        >
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.inspiration-banner.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:sections.inspiration-banner.settings.title.label"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.inspiration-banner.settings.image.label"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "t:sections.inspiration-banner.settings.image_alt.label"
    }
  ],
  "presets": [
    {
      "name": "t:sections.inspiration-banner.presets.name"
    }
  ],
  "templates": ["product"]
}
{% endschema %}
