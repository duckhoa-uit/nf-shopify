{{ 'single-material.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="color-{{ section.settings.color_scheme }} isolate gradient materials-section">
  <div class="section-{{ section.id }}-padding">
    <div class="page-width">
      {% render 'material-content', block: section, show_title_above_media_on_mobile: section.settings.show_title_above_media_on_mobile %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Single Material",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image",
      "info": "Displayed if no video URL is provided or as video poster"
    },
    {
      "type": "url",
      "id": "video_url",
      "label": "Video URL",
      "info": "Supports YouTube, Vimeo, or direct MP4 links. Will override image if provided."
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Material Title"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Material description</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button label"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button link"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "background-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 36
    },
    {
      "type": "checkbox",
      "id": "show_title_above_media_on_mobile",
      "label": "Show title above media on mobile",
      "default": false,
      "info": "When enabled, the title will appear above the media on mobile devices"
    }
  ],
  "presets": [
    {
      "name": "Single Material",
      "settings": {
        "title": "Premium Materials",
        "description": "<p>Discover our carefully curated selection of sustainable materials that combine durability with exceptional comfort.</p>",
        "button_text": "Learn more",
        "button_url": "#"
      }
    }
  ]
}
{% endschema %}
